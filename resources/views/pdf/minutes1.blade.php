<!DOCTYPE html>
<html>
<head>
    <title>Hi</title>
</head>
<body>
    {{-- <h1>{{ $meeting['title'] }}</h1> --}}
    {{-- <p>{{ $date }} - {{$time_from}} to {{$time_to}}  </p> --}}
    {{-- <p>{{ $meeting['venue'] }}</p> --}}

    {{-- <h2>Attendees</h2>
    <ol>
        @foreach ($attendees as $attendee)
            <li>{{ $attendee }}</li>
        @endforeach
    </ol> --}}

    {{-- <h2>Absentees</h2>
    <ol>
        @foreach ($absentees as $absentees)
            <li>{{ $absentees }}</li>
        @endforeach
    </ol> --}}

    {{-- <h2>Agendas</h2>
    <ol>
        @foreach ($agendas as $agenda)
            <li>{{ $agenda['title'] }} - {{ $agenda['presenter_id']}}</li>
        @endforeach
    </ol> --}}









</body>
</html>
